services:
  app:
    build: .
    command: pytest -v
    env_file:
      - .env.test
    depends_on:
      - db

  db:
    image: postgres:15
    environment:
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
      POSTGRES_DB: test_db
